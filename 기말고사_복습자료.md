# AI이노베이션 기말고사 복습자료

> **목적**: 하반기에 다룬 내용을 혼자서도 복습할 수 있게 정리한 학습 자료다. 실제 시험 문제가 아니라, 핵심 개념·표·계산을 한 흐름으로 다시 짚는 데 목적이 있다.
> **다루는 범위**
> - 검색 후처리 · 응답 합성 · 출력 구조화 · Query Engine · ChatEngine · Agent
> - 컴포넌트 커스터마이징 · 로컬 LLM · Tracing/Evaluation · 배포 · 프롬프트 엔지니어링
> - AI Engineering Stack · Foundation Model · 평가 방법론 · Evaluation-Driven RAG
> - 기말 프로젝트 — RAG Retrieval Live Evaluation (SciFact · `nDCG@10`)

---

## Part 1. 검색 결과 후처리 (Node Postprocessor)

### 1.1 왜 후처리가 필요한가

리트리버는 질의 벡터와 *가까운* Node를 골라 줄 뿐, 그 Node가 *유용한지*는 보장하지 못한다. 다음 세 가지가 RAG 품질을 무너뜨린다.

1. **노이즈 혼입** — 점수는 높지만 의도와 동떨어진 Node가 LLM을 헷갈리게 한다.
2. **순서 불일치** — Top-k에 정답이 있어도 뒤쪽에 있으면 무시된다 ("lost in the middle").
3. **민감 정보 노출** — *Confidential* Node가 그대로 프롬프트에 합쳐질 수 있다.

LlamaIndex는 이를 **Node Postprocessor** 하나로 푼다. `리트리버 → Postprocessor → Response Synthesizer` 사이에 끼우는 가벼운 변환기다.

### 1.2 후처리기의 세 가지 동작 모드

| 모드 | 하는 일 | 대표 클래스 |
|---|---|---|
| **Filter (필터링)** | 조건 미달 Node 제거 | `SimilarityPostprocessor`, `KeywordNodePostprocessor` |
| **Transform (변환)** | Node 내용 가공 (메타데이터 치환, 문장 압축) | `MetadataReplacementPostprocessor`, `SentenceEmbeddingOptimizer` |
| **Re-rank (재정렬)** | 질의-Node 관련도 재계산 후 재정렬 | `SentenceTransformerRerank`, `LLMRerank`, `CohereRerank` |

세 모드는 직렬로 체인하며, 보통 **filter → transform → re-rank** 순으로 쌓는다 (잡음 제거 → 내용 압축 → 정렬).

### 1.3 Sentence Window Retrieval — Transform의 대표 패턴

핵심 충돌: **벡터 검색은 청크가 작을수록 정확**하지만, **LLM은 문맥이 넓어야 답을 잘 만든다**.

해법: 임베딩은 한 문장 단위로, LLM 입력은 그 문장 ±N문장 윈도로 분리한다.

- `SentenceWindowNodeParser`가 문서를 문장 단위 Node로 쪼개고, 각 Node의 `metadata["window"]`에 앞뒤 N문장을 저장.
- 검색은 짧은 `node.text`(단일 문장)로 진행 → 정확도 ↑.
- 검색 후 `MetadataReplacementPostprocessor`가 `text`를 `metadata["window"]`로 교체 → LLM은 풍부한 윈도 텍스트를 받음.

| 항목 | `SimilarityPostprocessor` (Filter) | `MetadataReplacementPostprocessor` (Transform) |
|---|---|---|
| 동작 | 점수 미달 Node *제거* | `text`를 메타데이터 값으로 *교체* |
| Node 개수 | 줄 수 있음 | 변하지 않음 |
| 본문 길이 | 변하지 않음 | 늘거나 줆 |
| 대표 사용처 | 임계값 컷, 잡음 차단 | Sentence Window 검색 |

### 1.4 Re-ranker — 작은 모델로 큰 품질 향상

벡터 유사도는 빠르지만 정교하지 않다. **Re-ranker**(보통 cross-encoder, 예: `BAAI/bge-reranker-base`)는 "(질의, Node) 쌍"을 입력으로 받아 0–1 관련도를 출력한다.

```
리트리버: top_k=20 (넉넉히)  →  Re-ranker: top_n=5 (정밀하게)  →  LLM
```

임베딩은 의미를 *대략* 비교하지만 cross-encoder는 *직접* 비교하므로, **넓게 뽑고 re-ranker로 좁히는** 전략이 일반적으로 품질을 올린다.

---

## Part 2. 응답 합성 (Response Synthesizer)

### 2.1 하는 일

리트리버가 Node 리스트를 주면, Response Synthesizer는 LLM을 *몇 번*, *어떤 프롬프트*로 호출할지 결정한다. 이 선택지를 추상화한 것이 `response_mode`다.

### 2.2 7가지 합성 모드

| 모드 | 동작 | LLM 호출 수 | 적합한 상황 |
|---|---|---|---|
| `refine` | Node 1개씩 보며 답을 점진 정제 | $N$번 | 디테일 중요한 긴 답변 |
| `compact` | refine 로직 + Node 묶어 토큰 절약 | $\le N$번 | **기본 권장** |
| `tree_summarize` | Node를 트리로 재귀 요약 | $O(\log N)$번 | 대량 문서 요약 |
| `simple_summarize` | 잘라 붙여 한 번에 | 1번 | 빠르고 싼 요약 |
| `accumulate` | Node마다 답 따로 만들어 모음 | $N$번 | 비교/병렬 분석 |
| `compact_accumulate` | accumulate + compact | $\le N$번 | 다중 소스 효율 분석 |
| `no_text` | LLM 호출 없이 Node만 반환 | 0번 | 디버깅·검색 검수 |

### 2.3 모드별 핵심 동작 (질의 "When was the clock built?")

- **refine**: Node1로 임시답 생성 → Node2,3로 갱신 필요한지 차례로 질의. 무관하면 유지. $N$개 Node면 $N$번 호출, 비용 높음.
- **tree_summarize**: 그룹별 부분 요약 → 요약의 요약 → 하나 남을 때까지 반복. 보통 $O(\log N)$.
- **simple_summarize**: 모든 Node를 컨텍스트 한도에 맞게 잘라 한 번에. 1번 호출, 길면 디테일 손실.
- **compact_accumulate**: 각 Node에 대한 답을 모두 보존하되 한 호출로 묶음.

### 2.4 언제 무엇을 쓰나

- 챗봇 일반 응답 → `compact` (기본)
- 긴 보고서 요약 → `tree_summarize`
- "각 부서 의견 모두 보여줘" → `accumulate`
- 설계·디버깅 → `no_text`로 리트리버 품질만 확인

`response_mode`는 한 줄 변경이지만 **비용·지연·품질의 균형점을 옮기는 다이얼**이다.

---

## Part 3. 출력 구조화 (Output Parser & Pydantic Program)

### 3.1 동기

RAG 결과가 *다음 단계의 입력*(변수 저장, DB 적재, 함수 인자)이 되면 형식이 정확해야 한다. 자유 텍스트를 정규식으로 파싱하는 방식은 깨지기 쉽다. 두 갈래 해법:

1. **Output Parser** — 응답을 검사·재요청하는 *후방 검증기* (`LangchainOutputParser`)
2. **Pydantic Program** — 처음부터 Pydantic 클래스로 받게 하는 *전방 강제기* (`OpenAIPydanticProgram`)

### 3.2 JSON dict vs Pydantic 객체

| 측면 | JSON dict | Pydantic 객체 |
|---|---|---|
| 키 접근 | `quiz["x"]` (오타 시 런타임 KeyError) | `quiz.x` (오타 시 IDE/타입체커 차단) |
| 타입 보장 | 없음 | `int`/`str`/`list[str]` 자동 강제 |
| 누락 필드 | 사용 시점 KeyError | 생성 시점 `ValidationError` |
| 비즈니스 규칙 | 별도 if-문 | `@field_validator` 한 줄 |
| 메서드 | 못 붙임 | 가능 |
| IDE 지원 | 없음 | 자동완성·리팩토링 동작 |

요약: **JSON dict는 "일단 받아 두기", Pydantic은 "도메인 모델 인스턴스로 보장하기"**. RAG 결과가 후속 함수 인자가 되는 순간부터 후자의 가치가 커진다.

### 3.3 선택 기준

| 기준 | Output Parser | Pydantic Program |
|---|---|---|
| LLM 종속성 | 임의 LLM | Function Calling 지원 모델 |
| 구조 보장 | 사후 검증·재시도 | Function API 수준 강제 |
| 권장 | 레거시·다양한 LLM | 신규 프로젝트·타입 안전성 |

---

## Part 4. Query Engine · ChatEngine · Agent

### 4.1 Query Engine

자연어 질의 → 응답의 **엔드 투 엔드 인터페이스**. 안에 (a) 리트리버, (b) 후처리기, (c) 응답 합성기, (d) 출력 파서가 들어간다.

```python
query_engine = index.as_query_engine()   # 모든 기본값 적용된 RetrieverQueryEngine
```

저수준 조립 시 각 줄이 운영상 다이얼이 된다: `top_k`(비용/회수율), `cutoff`(정확도/회수율), `response_mode`(품질/비용).

**고급 Query Engine**

| 클래스 | 용도 |
|---|---|
| `RouterQueryEngine` | 질의에 따라 LLM이 어떤 엔진 쓸지 동적 선택 (description 기반 분기) |
| `SubQuestionQueryEngine` | "A와 B 비교" 같은 복합 질의를 하위 질문으로 분해 |
| `CitationQueryEngine` | 답변에 인용 표기 자동 추가 |
| `MultiStepQueryEngine` | 복잡 질의를 순차 단계로 분해 |
| `RetryQueryEngine` | 평가 미달 시 자동 재시도 |

### 4.2 ChatEngine — 메모리를 가진 Query Engine

Query Engine의 약점은 **대화 이력이 없다**는 것. ChatEngine은 *메모리가 달린 QueryEngine*이다. `ChatMemoryBuffer`가 토큰 한도 안에서 대화를 보관하고, 한도를 넘으면 **슬라이딩 윈도**로 오래된 메시지부터 잘라낸다.

**5가지 Chat Mode**

| Mode | 작동 방식 | 데이터 검색 | 적합한 상황 |
|---|---|---|---|
| `simple` | 순수 LLM 대화 | 안 함 | 데이터 없는 챗봇 |
| `context` | 매 질의마다 검색 → context 주입 | 매번 | 일반 RAG 챗봇 (가장 흔함) |
| `condense_question` | 대화 이력을 단일 질문으로 압축 → query_engine | 매번 | 후속 질문 잦을 때 |
| `condense_plus_context` | 압축 + context 병합 | 매번 | 정확도 최우선 (느림) |
| `react`, `openai` | 도구 가진 에이전트 모드 | 도구로 | 함수 실행 필요 시 |

- **`condense_question`**: "그 다음엔?" 같은 짧은 후속 질문을 *혼자서도 의미 통하는 질문*으로 다시 써서 검색. 단, 매번 검색하므로 "방금 뭘 물어봤지?" 같은 메타 질문에 약함.
- **`condense_plus_context`**: 검색은 압축 질문으로, 답변 생성 시점엔 원래 대화 이력까지 전달. 검색이 빗나가도 대화 이력이 답의 근거가 됨. 가장 정확하지만 가장 비쌈.

### 4.3 ReAct — 도구를 *언제* 쓸지 LLM이 결정

> **이름 주의**: 여기서 ReAct는 JavaScript의 React.js와 **무관**하다. *Reasoning + Acting*의 줄임말 (Yao et al., 2022).

**ReAct 루프** — 한 번에 답하지 않고 4단계를 반복:

1. **Thought** — 무엇을 알고 무엇이 부족한가 자문
2. **Action** — 어떤 도구를 부를지 결정
3. **Action Input** — 도구 입력값 생성
4. **Observation** — 도구 결과를 읽어 메모리에 추가

충분해질 때까지 사이클 반복 후 **Final Answer** 출력.

| 질문 유형 | `condense_question` | `condense_plus_context` | `react` |
|---|---|---|---|
| 데이터 질문 | 검색 → 답 | 검색 + 이력 → 답 | 도구 호출 → 답 |
| 모호한 후속 ("그 다음엔?") | 압축 질문으로 검색 | 압축 질문 + 이력 | 도구 호출 |
| 메타 질문 ("방금 뭘 물어봤지?") | 검색 헛돔, 답 약함 | 이력으로 정확히 답 | 도구 안 부르고 메모리로 답 |

### 4.4 Chatbot vs Agent — 결정적 차이

지금까지의 ChatEngine은 모두 **read-only**다. **에이전트**는 두 요소를 갖는다:

1. **도구(Tool) 모음** — 함수, API, DB 접근
2. **추론 루프** — "지금 어떤 도구를 써야 하나"를 LLM이 동적 결정

직관: 챗봇은 답을 주고, **에이전트는 일을 한다**.

**핵심 원칙**: 함수의 docstring과 `QueryEngineTool`의 description은 LLM이 도구 선택에 쓰는 *유일한 단서*다. 추론 루프의 정확도가 docstring 품질에 비례한다.

**두 가지 Reasoning Loop**

- `OpenAIAgent` — Function Calling API. 도구 선택이 모델 내부 로직. 안정적·빠름. function calling 지원 모델만.
- `ReActAgent` — Thought→Action→Observation을 프롬프트로 강제. 임의 LLM 가능, 프롬프트 품질에 민감.

`max_function_calls`는 *rogue agent*(무한 루프) 방지 안전 한도. 더 본질적 해법은 정확한 docstring.

### 4.5 종합 — 검색에서 행동까지

```
[질의] → [Retriever] → [Postprocessor] → [Response Synthesizer]
       → [Output Parser] → [QueryEngine] → [ChatEngine(+메모리)] → [Agent(+도구·추론루프)]
```

층이 올라갈수록 *능동적*이 된다. "검색해서 답하기"에서 "검색해서 답하고, 필요하면 행동하기"로.

---

## Part 5. 컴포넌트 커스터마이징과 로컬 LLM

### 5.1 OpenAI 기본값을 벗어나는 이유

1. **비용** — 토큰 단가 누적
2. **프라이버시** — 사내 문서 외부 전송 금지
3. **지연** — 외부 API 네트워크 왕복

### 5.2 Quantization (양자화)

32-bit 가중치를 더 좁은 표현으로 압축하는 사후 최적화. "밀가루 1.4732컵 → 1.5컵 반올림" 비유.

| 양자화 수준 | 메모리 | 속도 | 정확도 |
|---|---|---|---|
| FP32 (원본) | 가장 큼 | 가장 느림 | 가장 정확 |
| FP16 | 1/2 | 빠름 | 거의 동일 |
| INT8 | 1/4 | 더 빠름 | 약간 저하 |
| INT4 | 1/8 | 매우 빠름 | 다소 저하 |

**GGUF**는 양자화 가중치 저장·배포의 사실상 표준 포맷.

### 5.3 로컬 LLM·라우팅·교체

- **LM Studio**: llama.cpp + GUI. 핵심은 **OpenAI 호환 로컬 서버**(`http://localhost:1234/v1`). `api_base` 한 줄만 바꾸면 데이터가 외부로 안 나감.
- **LLM Routing** (Neutrino/OpenRouter): 단순 질문은 작은 모델, 복잡 추론은 큰 모델로 라우팅 → 비용 절감.
- **임베딩 교체**: `BAAI/bge-m3`(다국어, 8K), `multilingual-e5-large` 등. 임베딩만 로컬화해도 인덱싱 비용 0.
- **Llama Pack**: 흔한 패턴을 완성품 레고 블록으로. `ZephyrQueryEnginePack` 등.
- **Llama CLI**: `llamaindex-cli rag --files files/ -q "..."` — 코드 없이 즉석 RAG.

### 5.4 LLM 선택의 4가지 축

| 축 | 고려 사항 |
|---|---|
| Architecture | encoder-only(BERT) / encoder-decoder(BART) / decoder-only(GPT·Claude·LLaMA) / MoE(Mixtral) |
| Size | 7B(로컬) / 70B / 1.76T(GPT-4급) |
| Inference Speed | 작은 모델 + 양자화 + GPU offloading |
| Specialization | Chat / Instruct / Codex / Summarization / QA |

원칙: **"하나의 LLM으로 모든 걸"은 거의 항상 비효율**. 역할별 모델 분리가 비용·품질·지연을 모두 개선.

---

## Part 6. Tracing와 Evaluation

### 6.1 동기 — 추상화의 양면성

프레임워크는 RAG를 단순화하지만, 문제가 생겼을 때 *어디서 잘못됐는지* 보기 어렵다. 해법 둘:

- **Tracing** — 각 단계를 시간순 기록·시각화 (디버깅)
- **Evaluation** — 결과 자동 채점 (벤치마킹·회귀 검증)

### 6.2 Phoenix — Span과 Trace

| 용어 | 의미 |
|---|---|
| **Span** | 단일 작업의 시작·종료·메타데이터 (임베딩 1회, retrieve 1회) |
| **Trace** | Span들의 모음. 질의 한 건의 전체 경로 |

연동은 사실상 3줄: `px.launch_app()` → `set_global_handler("arize_phoenix")` → 질의. `localhost:6006`에서 kind(chain/retriever/llm/embedding…), latency, tokens 확인.

### 6.3 Phoenix 3가지 자동 채점

| 평가기 | 검사 대상 | 출력 |
|---|---|---|
| `RelevanceEvaluator` | retrieved Node가 질의에 적절한가 | relevant / irrelevant |
| `HallucinationEvaluator` | 응답이 컨텍스트에 충실한가 | factual / hallucinated |
| `QAEvaluator` | 응답이 질의에 정확한가 | correct / incorrect |

`provide_explanation=True`이면 점수뿐 아니라 *이유*를 받아 회귀 분석에 활용.

### 6.4 RAGAS — 참조 기반 정량 메트릭

Phoenix가 *심판형*이면 RAGAS는 *메트릭형*. 주요 메트릭: Recall@k, MRR, Faithfulness, Answer Relevance, Context Precision/Recall. 강점은 **reference-free**(정답 라벨 없이 LLM으로 채점) 가능.

**회귀 디버깅 루프**: 점수 낮은 Span 식별 → trace 점검 → retrieval/postprocess/synthesizer 중 원인 특정 → 해당 컴포넌트만 수정 후 재평가.

---

## Part 7. 배포와 프롬프트 엔지니어링

### 7.1 Streamlit 배포

파이썬 한 파일 = 웹 앱 하나. 핵심 패턴 3가지:

- `@st.cache_resource` — 인덱스 한 번만 로딩 (재빌드 비용 폭발 방지)
- `st.session_state` — 대화 메모리 유지
- `st.chat_message` / `st.chat_input` — 챗봇 UI 위젯

한계: 동시 사용자 처리·인증 부재. 학습·시연용으로는 충분.

### 7.2 프롬프트 시스템 — `get_prompts()` / `update_prompts()`

LlamaIndex가 호출하는 모든 LLM은 *템플릿* 기반. `{context_str}`, `{query_str}` 같은 변수 슬롯에 실행 시점 값이 끼워진다.

- `get_prompts()` — 현재 쓰는 프롬프트 들여다보기 (`response_synthesizer:text_qa_template` 등)
- `update_prompts()` — 프롬프트 교체. 예: `"and not prior knowledge"` → `"and any of your prior knowledge"` 한 구절 변경이 "모른다 vs 추정해서 답" 동작을 바꿈.

### 7.3 프롬프트 작성 황금률 7가지

1. **정확성·명확성** — "잘 정리해" < "5개 리스트, 각 1문장 이내"
2. **지시성** — Summarize/Analyze/Compare 같은 동사로 작업 명시
3. **컨텍스트 품질** — Garbage in, garbage out. 인덱싱·후처리가 여기서 다시 등장
4. **컨텍스트 양** — 너무 길면 cognitive load ↑. chunk_size 512~1024, top_k 3~10 출발점
5. **컨텍스트 순서** — *Lost in the middle*. `LongContextReorder`가 관련 높은 Node를 양 끝에 배치 (cf. Needle in a Haystack 테스트)
6. **출력 형식** — Output Parser/Pydantic Program으로 강제 ("JSON으로 답해" 텍스트만으론 부족)
7. **비용·지연** — 프롬프트 길수록 토큰·지연 ↑. 프롬프트 압축기·보수적 top_k

### 7.4 프롬프팅 기법 5가지

| 기법 | 핵심 | 출처 |
|---|---|---|
| **Zero-shot** | 예시 없이 바로 질의 | 기본 |
| **Few-shot (k-shot)** | $k$개 예시 후 본 질의 | Brown et al. (2020) |
| **Chain-of-Thought** | "단계별로 생각하라" + 추론 시연 | Wei et al. (2022) |
| **Self-Consistency** | 여러 CoT 답안을 다수결 | Wang et al. (2022) |
| **Tree of Thoughts** | 추론 단계를 트리로 탐색·평가·가지치기 | Yao et al. (2023) |

---

## Part 8. AI Engineering Stack과 Foundation Model

### 8.1 AI Engineering이란

이미 학습된 Foundation Model을 제품·업무 시스템에 연결하는 공학. 모델을 새로 학습시키는 일이 아니라, 문제 정의 → 모델 선택 → 데이터·prompt·retrieval·tool·평가를 묶어 작동하는 시스템을 만드는 일.

| 구분 | 전통적 ML Engineering | AI Engineering |
|---|---|---|
| 중심 자산 | task-specific model | foundation model + application layer |
| 개발 초점 | 학습 데이터·학습·metric | prompt·retrieval·tool·evaluation·guardrail |
| 출력 | 분류값·점수·예측값 | 자연어·코드·이미지·action plan |
| 실패 양상 | 낮은 정확도·overfitting | hallucination·prompt injection·latency·cost |

> 좋은 AI 시스템은 "무엇을 만들 것인가"와 "좋은 결과를 어떻게 측정할 것인가"를 **동시에** 설계한다.

### 8.2 Foundation Model의 위치

```
Language Model → Large Language Model → Large Multimodal Model → Foundation Model → AI Application
```

- **Masked LM**: 문장 중간 빈 token 맞힘 → 이해·분류·임베딩 계열
- **Autoregressive LM**: 이전 token 보고 다음 예측 → 생성·대화
- **self-supervision** 덕분에 사람이 모든 label을 안 붙여도 대규모 학습 가능
- Foundation Model은 단독으로 제품이 되지 않는다 — 주변에 데이터·검색·prompt·tool·router·cache·monitoring·feedback이 붙는다.

### 8.3 AI 애플리케이션 계획 — 판단 축

| 판단 축 | 설명 | 설계 영향 |
|---|---|---|
| Critical vs complementary | 없으면 제품이 안 되는가, 보조인가 | critical일수록 정확도·안정성 기준 ↑ |
| Reactive vs proactive | 요청에 반응하는가, 먼저 행동하는가 | proactive일수록 오작동 관리 중요 |
| Automation vs augmentation | 사람을 대체하는가, 보조하는가 | 자동화 높을수록 검증·human handoff 중요 |

### 8.4 AI Engineering Stack 3층

```
Application Development : prompt, context, RAG, tools, UI, evaluation
Model Development       : modeling, training, finetuning, inference opt, dataset eng
Infrastructure         : serving, data/compute mgmt, monitoring
```

학생 프로젝트가 직접 만지는 곳은 주로 **application development**(Document·Node·Index·Retriever·QueryEngine·MCP tool·endpoint).

### 8.5 Foundation Model 이해 핵심

- **Training Data**: 데이터 없는 언어·도메인은 약함. RAG가 필요한 이유 — pretraining에 없거나 최신이거나 내부 지식은 retrieval로 제공.
- **Multilingual**: 모든 언어가 같은 수준 아님. 한국어 RAG는 embedding 품질·PDF/HWP/OCR 파싱이 성능을 지배할 수 있음.
- **Architecture (Transformer)**: attention으로 token 간 관계 계산. context length·attention 비용·KV cache가 제약.
- **Model Size**: parameter만으로 판단 금지. Parameters(용량) / Training tokens(coverage) / FLOPs(학습 비용) 함께. **작은 모델 + 좋은 context + 좋은 retrieval + 명확한 prompt**로 충분한 품질 만들기가 핵심.
- **Sampling**: temperature(분포 날카로움), top_p, top_k, max_tokens. 평가·운영은 보통 temperature 낮춰 재현성 확보.
- **Structured Outputs**: 평가 endpoint는 schema 고정 필요. schema가 고정돼야 평가기·프론트엔드·로그 분석기가 안정 동작.

### 8.6 Post-Training — RLHF · DPO · RLAIF

Pretraining이 기본 지식을, **Post-training**이 지시 따르기·안전 응답을 만든다.

| 방식 | 핵심 아이디어 | preference signal | 특징 |
|---|---|---|---|
| **RLHF** | 사람 선호로 reward model 만들고 강화학습 | 사람 평가자 | 강력하지만 복잡·고비용 |
| **DPO** | 선호/비선호 쌍으로 모델 직접 최적화 | preference data | reward model·RL loop 불필요, 단순 |
| **RLAIF** | AI judge가 만든 선호로 조정 | AI judge | 대량화 쉬움, judge 편향 위험 |

예 (배송 지연 환불 질문): A "무조건 환불"(과도), B "주문번호·정책 확인 필요"(적절), C "고객 책임, 불가"(근거 없음). Post-training은 모델이 **B** 같은 답을 더 자주 내게 만든다.

- RLHF: B 선호 표시 → reward model 학습 → 점수화 → 강화학습.
- DPO: chosen=B, rejected=C로 직접 확률 조정 (중간 reward model 없음).
- RLAIF: AI judge가 기준으로 `B > A > C` 생성.

학생 포인트: 같은 base model이라도 어떤 preference tuning을 거쳤는지에 따라 거절 방식·JSON 준수·사실성 균형이 달라진다.

---

## Part 9. 평가 방법론

### 9.1 평가가 어려운 이유

| 난점 | 설명 | RAG 예 |
|---|---|---|
| 열린 출력 공간 | 좋은 답이 여러 개 | 같은 근거를 여러 방식 요약 |
| 그럴듯한 오류 | 문장은 자연스럽지만 근거 틀림 | 검색 문서와 답변 연결 끊김 |
| 다중 컴포넌트 | 파싱·chunking·retrieval·generation·serving 모두 영향 | metadata 누락이 전체 채점 실패 |
| 분포 차이 | 공개 benchmark ≠ 실제 업무 데이터 | 영어 우수 모델이 한국어 PDF에서 약함 |

### 9.2 평가 방식 4가지

- **Exact Evaluation** (모호하지 않은 비교): 다지선다 정답 비교, 코드 unit test, Text-to-SQL 실행 결과, endpoint schema, **retrieval `doc_id` vs qrels**. Retrieval 평가는 자연어 판단을 최소화 → 시스템 간 비교가 공정·재현 가능.
- **Reference-Based Evaluation**: 입력마다 정답/기준 응답 준비 후 유사도 측정. 번역의 reference translation, 코드의 test case, retrieval의 **qrels**.
- **AI as a Judge**: AI가 다른 AI 출력을 relevance·faithfulness·safety 등으로 평가. 정답 고정 안 되거나 reference 부족할 때 유용. 단 재현성·기준 모호성·비용·편향 한계. → retrieval 자동평가의 *주 지표*로는 부적합, 보조용.
- **Comparative Evaluation**: 두 시스템 출력 비교로 선호 관계 수집. Leaderboard가 이 운영 감각을 제공.

### 9.3 qrels — 검색 평가의 정답지

qrels = **query relevance judgments**. query마다 어떤 `doc_id`가 관련 문서인지 적은 표.

| `query_id` | `doc_id` | relevance |
|---|---|---:|
| Q1 | D1 | 1 |
| Q1 | D2 | 0 |

- `1` = 관련, `0` = 무관 (graded면 `2`=매우 관련, `1`=일부 관련)
- hidden qrels 전체를 공개 안 하는 이유: 평가 데이터 하드코딩 방지, 일반화되는 전략 측정
- 같은 정답 문서를 포함해도 **앞 순위에 둘수록** 좋은 ranking → 이 차이를 nDCG가 잡아냄

qrels가 중요한 이유: 자동 채점 가능 / 재현성 / 실패 분석. 잘못 만들면 평가도 잘못됨 (관련 문서 누락, 애매한 관련성을 모두 1로).

---

## Part 10. Evaluation-Driven RAG와 기말 프로젝트

### 10.1 Evaluation-Driven Development

TDD가 "테스트 먼저, 코드 나중"이면, EDD는 **"좋은 출력 기준 먼저, 시스템 나중"**.

| 질문 | 답 |
|---|---|
| 무엇을 평가하는가 | `/retrieve` 검색 결과 |
| 무엇을 평가하지 않는가 | 자연어 생성 답변 |
| 어떤 데이터로 | SciFact corpus + hidden query/qrels |
| 어떤 단위로 | query 하나의 ranked document list |
| 어떤 지표로 | 문서 단위 `nDCG@10` |
| 어떤 계약으로 | `rag_endpoint.json`, `GET /health`, `POST /retrieve` |

화려한 UI보다 **`doc_id` 보존, score 정렬, endpoint 안정성, timeout 대응**을 먼저 챙긴다.

### 10.2 왜 retrieval만 평가하나

generation을 평가하지 않으면:
- LLM 모델 차이·API 사용 여부·프롬프트 품질이 점수에 안 섞임
- hallucination judge 같은 불안정 평가 불필요
- 검색기가 정답 문서를 잘 찾는지 직접 측정

평가 단위는 **답변 문장**이 아니라 **문서 순위 목록**.

### 10.3 SciFact 데이터셋

과학 claim에 관련 있는 논문 초록을 찾는 retrieval 과제 (BEIR benchmark 포함). claim 참/거짓 분류는 하지 **않음**.

| 파일 | 역할 | 행 수 |
|---|---|---:|
| `corpus.jsonl` | 검색 대상 문서 전체 (`_id`/`title`/`text`) | 5,183 |
| `queries.jsonl` | claim/query 전체 (`_id`/`text`) | 1,109 |
| `qrels/train.tsv` | train query 정답 목록 | 919 |
| `qrels/test.tsv` | test query 정답 목록 | 339 |

- **train/test는 문서 split이 아니라 query/qrels split**. corpus는 항상 전체 하나를 RAG에 넣는다.
- qrels 행 수 > query 수인 이유: query 하나에 정답 문서가 여러 개일 수 있음.
- corpus의 `_id`를 검색 결과의 `doc_id`로 보내야 함.

### 10.4 Chunking 자유도와 doc_id 보존

```
학생은 chunk를 자유롭게 만든다.
평가 정답은 원본 문서 doc_id 기준.
학생 응답 chunk metadata에는 원본 doc_id가 반드시 있어야 한다.
```

**응답 계약** (`POST /retrieve`):

```json
{
  "query_id": "1",
  "contexts": [
    {"doc_id": "31715818", "chunk_id": "31715818::chunk_003",
     "score": 0.91, "text": "retrieved chunk text"}
  ]
}
```

필수: `contexts`, 각 context의 `doc_id`, `score`. 권장: `chunk_id`. 디버깅용: `text`.

**중복 doc_id 제거**: 같은 문서의 chunk가 여러 개 나오면 평가 서버는 **첫 등장만** 인정. 작게 잘라 같은 문서를 반복 반환하는 전략이 부당 이득을 못 얻게 함.

### 10.5 nDCG 계산 (손으로 풀 수 있어야 함)

**Binary relevance**: 정답이면 $rel(d)=1$, 아니면 0.

**Cumulative Gain** — 순서 무시:
$$CG@k = \sum_{i=1}^{k} rel(d_i)$$

**Discounted CG** — 뒤 순위에 할인:
$$DCG@k = \sum_{i=1}^{k} \frac{rel(d_i)}{\log_2(i+1)}$$

**Ideal DCG** — 관련 문서를 모두 맨 앞에 둔 ranking의 DCG.

**Normalized DCG** — 0~1로 정규화:
$$nDCG@k = \frac{DCG@k}{IDCG@k}$$

**할인값 빠른 참조**

| rank $i$ | $\log_2(i+1)$ |
|---:|---:|
| 1 | 1.000 |
| 2 | 1.585 |
| 3 | 2.000 |
| 4 | 2.322 |
| 5 | 2.585 |

**worked example — 정답 둘 (`A`, `C`)**, 결과 `[B, A, D, C]` → relevance `[0,1,0,1]`:

$$DCG@4 = 0 + \tfrac{1}{\log_2 3} + 0 + \tfrac{1}{\log_2 5} = 0.631 + 0.431 = 1.062$$
$$IDCG@4 = \tfrac{1}{\log_2 2} + \tfrac{1}{\log_2 3} = 1.000 + 0.631 = 1.631$$
$$nDCG@4 = \frac{1.062}{1.631} = 0.651$$

**전체 점수** — query $N$개 평균:
$$Score = \frac{1}{N}\sum_{j=1}^{N} s_j$$

### 10.6 왜 nDCG@10인가 (다른 지표와 비교)

- **Recall@K** = $\frac{|R \cap D_K|}{|R|}$ — 찾았는지는 보지만 순위 차이는 약하게 봄.
- **Precision@K** = $\frac{|R \cap D_K|}{K}$ — 순도를 보지만 정답 적으면 작게 나옴.
- **MRR** = $\frac{1}{N}\sum \frac{1}{r_j}$ — 첫 정답 위치만. **나머지 정답을 무시**.

MRR의 맹점 예 (정답 `A`,`C`):
- 시스템 A `[A, X, Y, C]`: MRR=1.000, nDCG@4=**0.877**
- 시스템 B `[A, C, X, Y]`: MRR=1.000, nDCG@4=**1.000**

MRR은 둘을 똑같이 보지만 nDCG는 두 번째 정답 위치까지 반영해 B를 더 높게 평가한다.

| 기준 | nDCG@10의 장점 |
|---|---|
| 순위 반영 | 관련 문서가 앞일수록 높은 점수 |
| 여러 정답 | query 하나에 정답 여러 개여도 자연 계산 |
| query별 정규화 | 정답 수 달라도 0~1로 비교 |
| BEIR 관례 | BEIR 주요 retrieval 지표 |
| RAG 적합성 | LLM에 들어갈 상위 context 품질 직접 반영 |

### 10.7 배포·디버깅 체크리스트

**배포**: 서버 `0.0.0.0` bind / Docker port mapping / Cloudflare Quick Tunnel 실행 / public `/health`·`/retrieve` 열림 / `rag_endpoint.json`을 `main`에 push (URL 끝에 `/health`·`/retrieve` 붙이지 않음).

**점수 낮을 때 확인 순서**:
1. `/health` 실패 → 2. `rag_endpoint.json` URL 최신 여부 → 3. `/retrieve` schema 오류 → 4. `contexts` 누락 → 5. `doc_id` 누락/오류 → 6. corpus ingest 누락 → 7. query 전처리 오류 → 8. chunk size 과소/과대 → 9. top_k 부족 → 10. reranking 부재

**Leaderboard 필드**: `latest_score`(최근), `best_score_so_far`(최고 기록), `latest_status`, `last_success_at`, `latest_error`. 서버가 꺼져 있으면 `latest_score`는 0에 가까워지지만 `best_score_so_far`는 남는다.

---

## Part 11. 한 학기 큰 그림

```
[Document] → 인제스트 → [Node] → 인덱싱·임베딩 → [Index]
  → 리트리버 → [Retrieved Nodes] → 후처리·재정렬 → [Refined Nodes]
  → 응답 합성 → [Response] → 출력 파서 → [Structured Output]
  → ChatEngine·Agent → [Conversational/Agentic App]
  → 커스터마이징·배포 → [Production RAG]
  → 평가 체계 → [Evaluation-Driven RAG]
```

운영 품질을 좌우하는 결정은 *추상화 안의 작은 다이얼*에 있다 — chunk_size, top_k, similarity_cutoff, response_mode, prompt template. 이 다이얼을 **측정하면서** 돌리는 것이 평가 루프(Phoenix·RAGAS·nDCG)의 본질이다.

**책임 있는 RAG**: 출처 표기(`CitationQueryEngine`), 민감어 필터링(`KeywordNodePostprocessor`), 로깅·감사(Phoenix trace), 모델 드리프트 감시(정기 재평가).

---

## 핵심 용어 빠른 점검

- **Node Postprocessor** 3모드: Filter / Transform / Re-rank
- **response_mode**: compact(기본) / refine / tree_summarize / accumulate / no_text
- **Output Parser**(후방 검증) vs **Pydantic Program**(전방 강제)
- **Chat Mode 5종**: simple / context / condense_question / condense_plus_context / react
- **Chatbot(read-only)** vs **Agent(도구 + 추론 루프)**
- **ReAct**: Thought → Action → Action Input → Observation 루프 (≠ React.js)
- **Quantization**: FP32→FP16→INT8→INT4, 저장 포맷 **GGUF**
- **Span/Trace**, **Phoenix 3평가기**(Relevance/Hallucination/QA), **RAGAS**(reference-free)
- **프롬프트 황금률 7**, **프롬프팅 기법 5**(Zero/Few-shot, CoT, Self-Consistency, ToT)
- **AI Engineering Stack 3층**: Application / Model / Infrastructure
- **Post-training**: RLHF / DPO / RLAIF
- **평가 4방식**: Exact / Reference-based / AI-as-Judge / Comparative
- **qrels**, **nDCG@10**, **중복 doc_id 제거**, **train/test = query split (문서 split 아님)**
